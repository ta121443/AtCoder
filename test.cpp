#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for(int i=0; i < (int)(n); i++)

int main(){
    int N, K;
    cin >> N >> K;

    vector<string> S(N);
    rep(i, N){
        cin >> S.at(i);
    }

    vector<int> same_num(26, 0);
    


}